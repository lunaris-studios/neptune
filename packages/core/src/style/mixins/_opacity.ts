import { css } from "styled-components";

import * as Common from "~/common";

export const borderShadowOpacity = Common.theme(Common.Theme.MODE, {
  [Common.Mode.LIGHT]: "",
  [Common.Mode.DARK]: "",
});

export const dropShadowOpacity = Common.theme(Common.Theme.MODE, {
  [Common.Mode.LIGHT]: "",
  [Common.Mode.DARK]: "",
});